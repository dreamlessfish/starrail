# 购买自动化脚本使用说明

## 📁 文件夹结构

```
H:\starrail\
├── image_recognition_automation.py  # 核心图像识别类
├── purchase_automation.py            # 购买自动化脚本
├── get_coordinates.py                # 坐标获取工具
├── images\                           # 模板图像存放文件夹
│   └── purchase_button.png           # 购买按钮图像（你需要添加）
└── 使用说明_购买自动化.md            # 本文件
```

## 🚀 快速开始

### 第一步：安装依赖

```bash
pip install opencv-python numpy pyautogui keyboard
```

### 第二步：准备购买按钮图像

1. **截取购买按钮**
   - 打开游戏/应用，找到购买按钮
   - 使用截图工具截取购买按钮（只截取按钮本身，不要包含太多背景）
   - Windows 截图：按 `Win + Shift + S`

2. **保存图像**
   - 将截图保存为 PNG 格式
   - 文件名：`purchase_button.png`（或你喜欢的名字）
   - 保存位置：`H:\starrail\images\purchase_button.png`

3. **图像要求**
   - ✅ 清晰、不模糊
   - ✅ 与屏幕上显示的按钮完全一致
   - ✅ 建议使用 PNG 格式（无损）
   - ✅ 图像不要太大（建议 50x50 到 200x200 像素）

### 第三步：确定搜索区域（可选但推荐）

为了提高识别速度和准确度，建议指定搜索区域。

#### 方法1：使用坐标获取工具（推荐）

1. 运行坐标获取工具：
   ```bash
   cd H:\starrail
   python get_coordinates.py
   ```

2. 操作步骤：
   - 将鼠标移动到搜索区域的**左上角**
   - 按 **F2** 键记录左上角坐标
   - 将鼠标移动到搜索区域的**右下角**
   - 按 **F2** 键记录右下角坐标
   - 工具会显示区域坐标，例如：`(100, 100, 800, 600)`
   - 按 **ESC** 退出

3. 将获得的坐标填入脚本：
   ```python
   SEARCH_REGION = (100, 100, 800, 600)  # 替换为你的坐标
   ```

#### 方法2：手动确定坐标

1. 使用 Windows 截图工具截图
2. 在图片上查看坐标（大多数图片查看器会显示鼠标位置）
3. 记录左上角和右下角坐标
4. 计算：`(左上角x, 左上角y, 宽度, 高度)`

### 第四步：配置脚本

打开 `purchase_automation.py`，修改以下配置：

```python
# 1. 购买按钮图像路径
PURCHASE_IMAGE = "images/purchase_button.png"  # 修改为你的文件名

# 2. 搜索区域（可选）
SEARCH_REGION = (100, 100, 800, 600)  # 或 None（全屏搜索）

# 3. 刷新按键
REFRESH_KEY = 'd'  # 修改为你需要的按键

# 4. 匹配阈值
THRESHOLD = 0.8  # 如果找不到，降低到 0.7 或 0.6

# 5. 最大尝试次数
MAX_ATTEMPTS = 100

# 6. 购买后等待时间
PURCHASE_DELAY = 2.0
```

### 第五步：运行脚本

```bash
cd H:\starrail
python purchase_automation.py
```

## 📖 详细说明

### 坐标确定方法

#### 方法1：使用坐标获取工具（最简单）

运行 `get_coordinates.py`：
- **F1**：获取当前鼠标坐标
- **F2**：获取区域坐标（按两次：左上角和右下角）
- **ESC**：退出

#### 方法2：使用 Python 代码获取

创建一个临时脚本：

```python
import pyautogui
import time

print("3秒后将显示鼠标坐标...")
print("将鼠标移动到目标位置")
time.sleep(3)

x, y = pyautogui.position()
print(f"当前坐标: ({x}, {y})")
```

#### 方法3：使用截图工具

1. 使用截图工具截图
2. 在图片查看器中查看坐标
3. 手动计算区域

### 图像命名规范

建议的命名方式：
- `purchase_button.png` - 购买按钮
- `refresh_button.png` - 刷新按钮
- `confirm_button.png` - 确认按钮
- `item_icon_1.png` - 物品图标1
- `item_icon_2.png` - 物品图标2

### 搜索区域格式

搜索区域使用元组格式：`(x, y, width, height)`

- `x`: 左上角 x 坐标
- `y`: 左上角 y 坐标
- `width`: 区域宽度
- `height`: 区域高度

示例：
```python
# 搜索屏幕左侧 800x600 的区域
SEARCH_REGION = (0, 0, 800, 600)

# 搜索屏幕中央区域
SEARCH_REGION = (400, 300, 800, 600)

# 全屏搜索（不推荐，速度较慢）
SEARCH_REGION = None
```

### 匹配阈值调整

如果脚本找不到图像，可以尝试：

1. **降低阈值**（更宽松）
   ```python
   THRESHOLD = 0.7  # 或 0.6
   ```

2. **提高阈值**（更严格，如果误匹配）
   ```python
   THRESHOLD = 0.9
   ```

3. **重新截取图像**
   - 确保图像与屏幕上的按钮完全一致
   - 检查是否有缩放、颜色变化等

### 刷新按键设置

可以修改为任何按键：
```python
REFRESH_KEY = 'd'      # D键
REFRESH_KEY = 'f5'     # F5键
REFRESH_KEY = 'r'      # R键
```

## 🔧 常见问题

### Q1: 找不到购买按钮怎么办？

**解决方法：**
1. 检查图像路径是否正确
2. 降低阈值（如从 0.8 降到 0.7）
3. 重新截取更清晰的图像
4. 检查搜索区域是否正确
5. 确保购买按钮在屏幕上可见

### Q2: 误点击了其他位置？

**解决方法：**
1. 提高阈值（如从 0.8 提高到 0.9）
2. 缩小搜索区域
3. 重新截取更精确的图像（只包含按钮，不要背景）

### Q3: 脚本运行太慢？

**解决方法：**
1. 使用搜索区域（不要全屏搜索）
2. 降低图像分辨率（不要太大）
3. 调整 `pyautogui.PAUSE` 值

### Q4: 如何停止脚本？

- **方法1**：将鼠标快速移动到屏幕左上角
- **方法2**：按 `Ctrl + C` 中断
- **方法3**：达到最大尝试次数后自动停止

### Q5: 坐标获取工具无法运行？

如果提示 `keyboard` 模块错误：
```bash
pip install keyboard
```

注意：`keyboard` 模块在某些系统上可能需要管理员权限。

## 📝 使用示例

### 示例1：基本使用（全屏搜索）

```python
PURCHASE_IMAGE = "images/purchase_button.png"
SEARCH_REGION = None  # 全屏搜索
REFRESH_KEY = 'd'
THRESHOLD = 0.8
```

### 示例2：指定区域搜索（推荐）

```python
PURCHASE_IMAGE = "images/purchase_button.png"
SEARCH_REGION = (100, 100, 800, 600)  # 只在指定区域搜索
REFRESH_KEY = 'd'
THRESHOLD = 0.8
```

### 示例3：低阈值匹配（图像可能有变化）

```python
PURCHASE_IMAGE = "images/purchase_button.png"
SEARCH_REGION = (100, 100, 800, 600)
REFRESH_KEY = 'd'
THRESHOLD = 0.7  # 降低阈值
```

## ⚠️ 注意事项

1. **测试前验证**：先用 `find_image()` 测试能否找到图像
2. **图像质量**：确保模板图像清晰，与屏幕上的按钮一致
3. **安全使用**：不要用于非法用途
4. **备份数据**：自动化操作可能影响系统，请谨慎使用
5. **遵守规则**：遵守游戏/应用的使用条款

## 🎯 工作流程

脚本的执行流程：

```
开始
  ↓
查找购买按钮（在指定区域内）
  ↓
找到？ ──是──> 点击购买 ──> 等待 ──> 完成
  ↓否
按D键刷新
  ↓
等待刷新完成
  ↓
继续查找（循环）
```

## 📞 技术支持

如果遇到问题：
1. 检查 Python 版本（需要 3.7+）
2. 检查所有依赖包是否正确安装
3. 检查图像路径和格式
4. 查看错误信息并调整参数

---

**祝使用愉快！** 🎉


