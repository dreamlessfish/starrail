# 项目介绍：星穹铁道《货币战争》自走棋辅助脚本

## 一、项目概述

这是一个面向《崩坏：星穹铁道》内置玩法《货币战争》的自走棋辅助脚本项目，核心目标是通过图像识别、状态建模和自动化操作，实现游戏内棋子识别、自动购买、自动卖出和自动刷新，从而降低重复操作成本，提升策略执行的稳定性和效率。

项目采用 Python 开发，结合 OpenCV、PyAutoGUI、Pandas 等库，构建了一个“图像识别 + 策略表决策 + 鼠标键盘自动化执行”的完整闭环。

## 二、项目背景

《货币战争》玩法中，玩家需要频繁执行以下操作：

- 识别商店内目标棋子
- 判断当前手牌和商店状态
- 决定是否购买、刷新或卖出
- 在出现三星棋子后及时执行卖出操作

这些操作重复度高、节奏快，对手动操作的稳定性要求较高。为解决这一问题，我设计并实现了该辅助脚本，将原本依赖人工判断和点击的流程自动化。

## 三、核心功能

### 1. 自动识别商店目标棋子

- 通过模板匹配识别商店区域中的目标棋子
- 支持同时识别多个目标图像
- 可按指定阈值控制识别精度

### 2. 自动购买与刷新

- 当检测到目标棋子时自动点击购买
- 未识别到有效目标时自动触发刷新
- 支持自定义刷新按键、点击间隔和等待时间

### 3. 自动识别并卖出三星棋子

- 检测手牌区域中的三星棋子
- 通过拖拽操作将三星棋子移动到卖出位置
- 形成完整的“购买 - 合成 - 卖出”自动循环

### 4. 策略表驱动决策

- 将手牌状态抽象为有限状态
- 预生成 Excel 策略表，实现离线查表决策
- 根据商店中目标棋子数量快速确定动作类型：
  - `Buy`
  - `Buy & Sell`
  - `Refresh`
  - `Sell`

### 5. 坐标采集与参数配置

- 提供独立坐标采集工具
- 支持快速标定商店区域、手牌区域和卖出坐标
- 便于适配不同分辨率和不同客户端布局

## 四、技术方案

### 技术栈

- Python
- OpenCV
- NumPy
- Pandas
- OpenPyXL
- PyAutoGUI
- Keyboard

### 架构拆分

项目主要拆分为以下几个模块：

#### `image_recognition_automation.py`

负责底层自动化能力：

- 屏幕截图
- 模板匹配
- 单目标 / 多目标识别
- 鼠标点击
- 鼠标拖拽

#### `purchase_automation.py`

负责图像触发型自动购买逻辑：

- 商店触发器检测
- 目标棋子批量购买
- 三星棋子卖出循环

#### `strategy_generator.py`

负责生成策略表：

- 枚举稳定手牌状态
- 模拟购买与合成结果
- 为每种状态生成动作和跳转状态
- 输出为 `strategy_table.xlsx`

#### `main.py`

负责策略表驱动主流程：

- 加载策略表
- 同步当前手牌状态
- 识别商店中目标棋子数量
- 查表获取动作与下一状态
- 执行购买、刷新和卖出

#### `get_coordinates.py`

负责辅助配置：

- 获取鼠标当前位置
- 记录屏幕区域坐标
- 便于快速完成环境适配

## 五、项目难点

### 1. 图像识别稳定性

游戏界面中的图像识别会受到分辨率、缩放比例、亮度变化和界面状态影响。为此我采用了：

- 限定区域识别，降低误匹配概率
- 阈值参数可配置，适配不同场景
- 多模板识别，兼容目标棋子不同外观

### 2. 状态决策复杂度

如果完全依赖运行时逻辑判断，购买、合成、卖出链路会较复杂。因此我将问题转化为：

- 先离线枚举所有稳定状态
- 预计算各状态在不同商店输入下的动作
- 运行时直接查表，提高决策效率和可维护性

### 3. 自动化操作时序控制

游戏内购买、合成和卖出都存在动画和界面刷新延迟。项目中对以下环节做了时间控制：

- 点击间隔
- 刷新后等待
- 购买后等待
- 合成动画等待
- 卖出后等待

这样可以减少误点、漏判和状态不同步问题。

## 六、项目亮点

- 将游戏内重复操作抽象为“识别 - 决策 - 执行”的自动化流程
- 使用 Excel 策略表替代部分实时复杂判断，降低主流程逻辑耦合
- 同时支持纯图像触发模式和策略表模式，两种自动化方案可独立使用
- 具备较强的可配置性，能够通过坐标和模板替换快速适配环境

## 七、我的个人贡献

如果这份项目是你独立完成，可以在简历中写成：

- 独立设计并实现《崩坏：星穹铁道》内置玩法《货币战争》自走棋辅助脚本
- 基于 OpenCV 模板匹配实现商店棋子识别、手牌识别和三星卖出检测
- 使用 PyAutoGUI 完成购买、刷新、拖拽卖出等自动化操作
- 将游戏状态抽象为有限状态机，并通过 Pandas + Excel 构建离线策略表决策系统
- 设计坐标采集与参数配置工具，提升脚本在不同分辨率环境下的可复用性

## 八、可直接放入简历的版本

### 版本一：简洁版

**星穹铁道《货币战争》自走棋辅助脚本**  
基于 Python 开发的游戏自动化项目，使用 OpenCV 进行图像识别，结合 PyAutoGUI 实现商店棋子自动识别、自动购买、自动刷新和三星棋子自动卖出。通过 Pandas + Excel 构建策略表，将手牌状态抽象为有限状态并进行查表决策，提升了自动化流程的稳定性和执行效率。

### 版本二：偏技术版

**星穹铁道《货币战争》自走棋辅助脚本**  
- 使用 Python、OpenCV、PyAutoGUI、Pandas 实现游戏界面自动化  
- 基于模板匹配完成商店目标棋子与手牌三星棋子的检测  
- 将购买 / 刷新 / 卖出逻辑抽象为有限状态机，并离线生成 Excel 策略表驱动运行时决策  
- 通过坐标采集工具和可配置参数实现不同分辨率下的环境适配  

### 版本三：偏项目成果版

**星穹铁道《货币战争》自走棋辅助脚本**  
针对游戏内高频重复的购买、刷新和卖出操作，开发了一套基于图像识别和策略表决策的自动化脚本。项目实现了目标棋子检测、状态同步、自动购买、三星卖出和流程时序控制，降低了人工重复操作成本，并提升了策略执行的一致性。

## 九、面试中可以这样介绍

如果面试官追问，你可以这样回答：

1. 这个项目本质上是一个基于图像识别的自动化系统，不只是简单连点器。
2. 难点不在点击本身，而在于如何稳定识别游戏状态，以及如何把购买、合成、卖出流程串起来。
3. 我没有把所有逻辑都写成运行时判断，而是把状态提前枚举成策略表，运行时直接查表，提高了决策速度，也让逻辑更清晰。
4. 我还单独做了坐标采集和参数配置，方便脚本在不同屏幕环境下复用。

## 十、适合强调的关键词

你可以根据岗位方向，从下面这些关键词中选择：

- Python 自动化
- OpenCV 图像识别
- 模板匹配
- GUI 自动化
- 状态机建模
- 策略表驱动
- 脚本工程化
- 参数化配置
- 工具开发

